---
title: EMS Timer 進度報告
audience: PM
last_updated: 2026-07-04
total_entries: 9
---

# EMS Timer 進度報告

> **產品**：EMS DoseSync Pro（救護人員給藥計時器）
> **最後更新**：2026-07-04
> **累計進度**：9 筆

## 章節導覽

- [進度 9 - 2026-07-04 - 沒有畫面變化的地基補強：時間戳型別防呆 + 醫療規則測試網 + 字型工具防坑（含電源白屏修復收尾）](#progress-2026-07-04)
- [進度 8 - 2026-05-25 - 實機 UI bug 5 隻一日清完（B1-B5 含 PROGMEM 載入陷阱 + Snapshot 漏欄位第三次踩）](#progress-2026-05-25)
- [進度 7 - 2026-05-24 - DS3231 RTC 整合上機（裝置從此有真實時間戳）](#progress-2026-05-24-2)
- [進度 6 - 2026-05-24 - Phase F BLE 鏈路打通端到端（韌體 → 藍牙 → 雲端落地）](#progress-2026-05-24)
- [進度 5 - 2026-05-12 - Phase F 網頁端完整 UI 落地（雲端 + SoT §17 全套）](#progress-2026-05-12-2)
- [進度 4 - 2026-05-12 - V1 各階段進度盤點＋測試基礎建設到位](#progress-2026-05-12)
- [進度 3 - 2026-05-11 - 用自動化測試掃出 10 個畫面文字溢出並修正](#progress-2026-05-11)
- [進度 2 - 2026-05-09 - 補登次數選單列表化＋介面文字溢出修整](#progress-2026-05-09-2)
- [進度 1 - 2026-05-09 - 螢幕升級到 2.8 吋並修整體美觀](#progress-2026-05-09)

---

## <a id="progress-2026-07-04"></a>進度 9 — 2026-07-04 — 沒有畫面變化的地基補強：時間戳型別防呆 + 醫療規則測試網 + 字型工具防坑（含電源白屏修復收尾）

### 一句話

這次**沒有新功能、也沒有畫面改動**，全是把之前趕功能欠下的技術債還掉：把裝置時間戳的底層資料結構改得更防呆（消滅一個會把「還沒對時」誤當成「1970 年」的陷阱）、替幾個醫療相關關鍵規則補上自動測試防護網、並修好一個字型工具會讓補的字沒真正進裝置的坑。全程 **435 項自動測試全綠**、韌體照常編譯、**行為完全不變**。順帶把從進度 7/8 延續的**電源電池白屏問題修復收尾**。

### 補了什麼（雖然畫面沒變，但地基更穩）

| 補強項 | 為什麼對產品重要 |
|--------|------------------|
| **時間戳資料結構防呆** | 以前「裝置還沒對時」跟「時間剛好是 1970-01-01」在程式裡長得一模一樣，未來擴充容易誤判。現在型別上分乾淨 → 接手機 App 時不會把「沒對時的紀錄」誤當成有效時間 |
| **醫療規則測試網（+13 項測試）** | 「案件開始沒對時、中途才對到時間」「案件結束要自動停 6 秒通氣提示」這些規則以前沒有自動測試守著，改壞了不會被發現。現在有測試鎖死，未來動到就會被抓 |
| **字型工具防坑** | 進度 8 的 B1 缺字就是「補了字但裝置上還是舊字型」造成的。這次讓工具在出問題時**大聲報警**，不會再默默留下沒同步的舊字型 |

### 過程中的做法

- 沿用前面「**把邏輯抽成純函式再測**」的手法：要測的醫療規則本來埋在一個牽連 20 幾個全域變數的大函式裡，硬測不切實際；抽出來變成獨立小函式後就乾淨可測，裝置端照樣呼叫、行為不變
- 兩位獨立的自動審查員（一個看程式風格、一個看測試品質）都審過這批，確認測試**真的能在程式退步時抓到問題**，不是擺好看的

### 工程數字

- 自動測試 429 → **435 項全綠**（唯一 1 個未過是既有的硬體相關測試在電腦上本來就跑不了，與本次無關）
- 韌體編譯正常，容量占用 **68.6%**（不變）
- 共 9 筆改動，多筆是「抽純函式讓它可被測試」的重構

### 電源白屏修復收尾

✅ 從進度 7/8 延續的**純電池供電開機白屏/閃爍**問題已修復：先前量測定位到病灶是**負極/地線接觸不良**，帶載時吃掉約 0.5V 電壓，害裝置卡在低電壓保護邊緣（正極與升壓板都正常）。這次把地線接點焊死後**實測通過**，純電池供電可穩定開機。

### 順帶：SoT V1 功能完成度盤點（更新自進度 4 的 2026-05-12 舊表）

SoT V1 規格 8 個階段（A~H）截至今天的完成度 —— **臨床核心 5 個 Phase 全綠，剩 3 個輔助 Phase**：

| 階段 | 功能 | 狀態 |
|------|------|------|
| **A** OHCA 核心 | EPI 4 分循環 / 兩段確認 / 預警警報超時 | ✅ 完成 |
| **B** 補登＋Amio＋總覽 | 補登 / 藥物 / 案件總覽 / 事件時間軸 | ✅ 完成 |
| **C** 6 秒通氣 | 獨立模式 + OHCA 內快速切入 | ✅ 完成 |
| **E** 持久化＋歷史 | 重開機不遺失 + DS3231 真實時間戳 | ✅ 完成（DS3231 已補上，舊表的「缺硬體」已解）|
| **F** App 同步 | BLE 配對碼 + 單案同步 | ✅ 完成 |
| **D** 訓練模式 | 模擬訓練流程 | ⬜ **未開工**（純韌體、下次重點；目前主選單為佔位畫面）|
| **G** 系統設定＋工具 | 設定 / 裝置名稱 / 音量亮度 / Type-C 電腦端工具 | ⬜ **未開工**（純韌體、下次重點；目前主選單為佔位畫面）|
| **H** 電源管理 | 充放電 / 電池顯示 / 低電量警告 | ⏸ **卡硬體**（電池已有，但電量顯示受限於 ADC 腳已用盡）|

> 💡 **還差多少**：臨床救護核心 100% 完成。剩 **D + G 純韌體可自主推進**（做完裝置端 5 個主選單全部可用，目前訓練 / 設定 2 項還是佔位畫面）；**H 卡硬體**。粗估裝置端主功能完成度約 70-75%，差的都是輔助功能、非急救核心。

### 後續工作

- ⬜ **下次開工：Phase D（訓練模式）+ Phase G（系統設定 / 裝置名稱 / 音量亮度 / Type-C 工具）** — 純韌體、無阻塞
- ⬜ **DS3231 永續性測試**（鈕扣電池備援，約 5 分鐘可完成）
- ⬜ **剩餘畫面美化債**（3 項，非急，部分等 PM 實機確認）
- ⬜ **React Native App**（取代純網頁的長期方案）
- ⬜ **電源模組整合測試**（滿電拔線連續運作 ≥ 30 分等）

> 💬 這一筆是「量產前把地基打穩」的一步：功能面已達階段 1 里程碑（進度 6-8），這次專注在讓底層資料結構、醫療關鍵規則、工具鏈更耐操，降低後續接 App 與量產時踩坑的機率。

---

## <a id="progress-2026-05-25"></a>進度 8 — 2026-05-25 — 實機 UI bug 5 隻一日清完（B1-B5 含 PROGMEM 載入陷阱 + Snapshot 漏欄位第三次踩）

### 一句話

5/24 實機驗收抓出的 **4 個 UI bug**（缺字 / 疊字 / 游標位移 / 結束前檢查游標）+ 同時段發現的 **6 秒給氣 LOCKED 殘留閃爍**（B5）一天內全部修完通過 PM 實機驗證。途中**揭開兩個重要工具鏈陷阱**：字型實機載入走 PROGMEM 不是 LittleFS（過去 10 天踩坑無人察覺）、DisplaySnapshot 漏欄位 bug 第三次重演。修完跑 POST-COMMIT-REVIEW 五步驟結案並推 origin。

### 驗收結果

| Bug | 症狀 | PM 實機驗證 |
|-----|------|------------|
| **B1** dialog + 案件總覽缺「狀 態 ： 至」字 | 中文字顯示成 □ tofu 方塊 | ✅ 通過 |
| **B2** 案件總覽標題 / 同步狀態 / EPI 三行重疊 | 整個畫面擠成一團看不清 | ✅ 第二輪通過 |
| **B3** 返主選單後游標 highlight 跟文字錯位 | 反白框跟字 Y 軸沒對齊 | ✅ 通過 |
| **B4** 結束前檢查第一次按 UP 不動要按兩次 | 預設「返回案件」UP 沒反應 | ✅ 通過 |
| **B5** OHCA 結束進 LOCKED 後畫面反覆閃 | 結束前開過 6 秒給氣留下殘留 | ✅ 通過 |

### 每個 bug 的真根因（給 PM 的版本）

- **B1 — 字型實機載入走 PROGMEM 不是 LittleFS**
  字型字符明明存在，但燒進去仍缺字。挖了兩輪才發現裝置實際讀取的是嵌進 firmware binary 的 C 陣列（`ems_zh_24_vlw.h`），不是 LittleFS 的 `.vlw` 檔。過去 `regen_vlw.sh` 只生成 `.vlw` 不更新 `.h`，導致 commit `e79f4f9` (5/24) 寫「字型」但實際補的字根本沒進 binary。**自 5/15 commit `078d3b2` 之後 header 整整 10 天沒更新**，連兩次都被誤判修好。修法：新增自動 `.vlw → .h` 轉換工具，整合進 `regen_vlw.sh` 流程，未來不會再漏

- **B2 — 案件總覽 9 行內容塞不下 240px**
  原始 layout 用 LINE_H=22 + 1.0× 字級，9 行內容（標題、同步狀態、EPI 3 行、電擊 2-3 行、Amio、底部 2 個 sub-menu）總高度超過螢幕高度。同時 `SYNC_STATUS_Y` 只設「標題下方 14px」對 1.2× 大標題（29px 高）不夠避開。第一輪修把同步狀態避開標題 + 字級縮 0.85× + 行高 18 解前段，第二輪發現 Amio 仍撞 sub-menu，再調 sub-menu 起點下移 18px 完成

- **B3 — 主選單與其他畫面用不同文字渲染 API**
  drawMainMenu 用 `setCursor + print` 走 Print stream 接口，其他畫面用 `drawString + setTextDatum` 走 datum 接口。中文字型下兩者的 (x, y) 含義不同 — Print stream 走 baseline-relative，drawString 走 datum-relative，跟反白框的 raw Y 座標含義不一致 → 文字跟反白框錯開。改用統一 API 解決

- **B4 — DisplaySnapshot 漏欄位 bug 第三次重演**
  畫面去重機制用 memcmp 比對 snapshot，但 snapshot struct 沒含 `endCheckCursor` → 按 UP 改 cursor 但 snapshot 看不到變化 → 跳過重繪。同樣的坑 Phase E 漏 `historyCursor` / Phase F 漏 `summarySubmenuCursor` 都踩過，這是**第三次**。加進 snapshot 並補 regression test 鎖死；同時存進團隊記憶庫制定「新加 UI state 必跑 5 步驟 checklist」避免第四次

- **B5 — 規格 §14.12「結束後自動停止」韌體只做半套**
  原本案件結束渲染條件已排除 LOCKED/SUMMARY 不畫給氣 overlay，但內部 timer (`ventStartMs`) 沒清 → 每秒算出新 beat → snapshot 仍每秒變化 → 觸發全螢幕重畫 → 視覺閃爍。**不是 overlay 反覆出現，是底層重繪反覆觸發**。修法：進 LOCKED 時清掉 timer，timer + 渲染條件一致對齊「結束後自動停止」spirit

### 工程數字

- 7 commit 修 5 個 bug + 2 commit 文件 + 1 commit simplify cleanup + 1 commit POST-COMMIT-REVIEW IMPORTANT 修
- 新工具 `firmware/tools/vlw2header.py`（.vlw → PROGMEM C array）
- 新測試 `test_end_check_cursor_change_triggers_redraw_b4_regression`（B4 regression）
- 抽 `resetOhcaVentState()` helper 消除 ohca_logic 兩處重複
- POST-COMMIT-REVIEW 全 5 步驟跑完（範圍 `7f81f6b..af5f54a`）：simplify cleanup + 5-agent 並行 review，0 CRITICAL / 3 IMPORTANT 修 / 評分 **28/30**
- native test：419 / 420 PASSED（baseline test_storage_hw ERROR 既有）
- 主韌體 Flash：68.6%（不變）
- 全部 21 個 commit 推 origin（GitLab）

### 抓出的工具鏈債（已修 + 入記憶庫）

| 陷阱 | 影響 | 對策 |
|------|------|------|
| **`.vlw` 跟 `.h` header 未同步** | 改字型沒生效，連 2 次誤判修好 | `regen_vlw.sh` STEP 09 自動同步；commit 前驗 `_len` 對得上 |
| **DisplaySnapshot 漏 cursor 欄位**（第三次踩） | 新加 UI state 沒重繪 | 5 步驟 checklist（struct / Inputs / capture / main.cpp input / regression test）入記憶庫 |
| **drawMainMenu setCursor+print vs drawString datum 不一致** | 主選單對齊問題 | 統一改 drawString + datum；其他 menu (drawBackfill* / drawQuickMenu) 留 PM 實機觀察 |

### 已知未修（留 backlog）

- ⏳ **drawOhcaSummary `drawKVRow` helper** — 6 段重複可抽出減 60 行
- ⏳ **layout magic numbers 衍生**：`+32 / 78 / 18 / 0.85f` 從字型 size 衍生避免改字級時 drift
- ⏳ **B3 datum fix 可能未傳播到其他 4 個 menu**（drawBackfillCategory / Type / Count / QuickMenu）— 等 PM 實機確認後再修
- ⏳ **B5 `dispatchOhcaEvent` 進 LOCKED reset vent state 缺 native test** — 需 stub 10+ globals overhead 大，PM 已實機驗
- ⏳ **DS3231 永續性測試（CR2032 備援）** — 5 分鐘可完成

### 後續工作

- ⬜ **DS3231 永續性測試**（從進度 7 延續）
- ⬜ **3 項 Phase F 強化測試補完**（中斷重連 / 50 events 滿載 / Training case mode）
- ⬜ **React Native App**（取代純網頁的長期方案，與網頁共存）
- ⬜ **Group 2C UI 反饋 PM 對齊**（失敗哲學 A/B/C 三選一）

> 💬 進度 6（BLE 鏈路）+ 進度 7（DS3231 時鐘）+ 進度 8（UI 收尾）= 階段 1 主要里程碑全部達成。剩餘是量產前的硬體驗證 + Training mode + 系統設定 + 電源管理。

---

## <a id="progress-2026-05-24-2"></a>進度 7 — 2026-05-24 — DS3231 RTC 整合上機（裝置從此有真實時間戳）

### 一句話

**裝置現在有可信時間了**：DS3231 即時時鐘模組接上 I2C bus，boot 時自動偵測 → 把硬體時間餵給軟體時鐘 → OHCA 案件起訖時間從原本的 0 變成真實 epoch；同一份韌體在沒接 DS3231 的板子上仍可運作（自動降級走 BLE 對時），不需重編。**進度 6 末段標註的「同步後案件無真實開始時間」已解決**。

### 驗收結果

| 驗收項目 | 結果 |
|---------|------|
| 接 DS3231 boot：偵測到 0x68 + seed 軟體時鐘 | ✅ |
| 跑一筆 OHCA 案件 → LOCKED → 雲端 `case_start_ms` / `case_end_ms` 是真實 epoch | ✅ |
| BLE App 對時 → 反向寫回 DS3231（App 為主時鐘源） | ✅ |
| BLE `time_sync_ack` `source` 欄位 = `"ble+rtc"`（之前是 `"ble"`） | ✅ |
| 不接 DS3231 在同一份 binary 上 boot：掛 NullRtcBackend 降級走 BLE 對時 | ✅ |
| 永續性：對時 → 斷電 30 秒 → 重開 → 不需 BLE 就 seed | ⏸ **待測**（CR2032 紐扣電池備援能力未實測）|

### 過程怎麼做的（給 PM 的版本）

採 **runtime 偵測 + 雙模式 backend** 設計（不是 compile-time flag）：

- **同一份韌體**：開機時試問 DS3231 在不在；在就用真實時鐘，不在就降級到原本的「靠 App 連線對時」模式
- **App 對時 → 寫回 RTC**：救護人員開 App 那一刻就等同對時；DS3231 永遠是最新時間，下次斷電重開不會失憶
- **不在線也能跑**：沒插 DS3231 的板子直接用，行為跟 Phase F 結案時一樣（事件用相對毫秒，App 端重建時間）

### 設計重點

- **抽象介面** `RtcBackend`：3 個 method（`is_present` / `now_epoch_ms` / `set_epoch_ms`），上層 caller 不分支處理硬體在不在
- **三種實作**：
  - `DS3231Backend`：用 Adafruit RTClib 包裝 0x68 I2C 通訊（硬體相依，僅 ARDUINO env 編譯）
  - `NullRtcBackend`：純降級 stub，所有讀取回 0、寫入 no-op
  - `MockRtcBackend`：native test 注入用，可控 present 與 advance_ms
- **`ems_time_sync_seed_from_rtc`** API：把 RTC 讀到的 epoch 灌進既有軟體時鐘 state，下游 `current_epoch_ms()` 自動受益
- **boot seed 三分支**：DS3231 在線 + 有時間 → seed；DS3231 在線但未設過 → 等 BLE；不在線 → 降級
- **TDD 6 wave**：抽象介面 → seed API → integration（mock backend）→ DS3231 包裝 → 主程式 wire up → 文件，每 wave RED → GREEN → 防退步全 suite

### 工程數字

- 新 lib：`firmware/lib/ems_rtc/`（4 檔，純邏輯 + 硬體包裝分開）
- 新 test：`test_rtc` + `test_rtc_integration` 共 10 個 native test cases
- 既有 `test_time_sync` +3 cases（seed API）
- 全 suite：**418 / 419 PASSED**（同基準，唯一 ERRORED 為 pre-existing `test_storage_hw` 硬體相依）
- 主韌體 Flash：67.3% → 67.9%（+12KB RTClib 實際連入）

### 過程中遇到並解決的問題

- ✓ **RTClib 與 LovyanGFX 並存時 PlatformIO 找不到 SPI.h**：Adafruit_BusIO 透過 SPI 抽象 I2C，與 LovyanGFX 並用時 LDF 預設 chain+ 模式不會把 framework 的 SPI/Wire include path 帶進 BusIO 編譯。試過顯式 `lib_deps += SPI/Wire` 無效，改 `lib_ldf_mode = deep+` 解決
- ✓ **常數重定義**：`I2C_SDA_PIN` / `I2C_SCL_PIN` 已在 `app_globals.h:148-149` 宣告；`SYNCED_AT_EPOCH_FLOOR_MS` 已在 `ems_storage_logic.h:123` 宣告。沿用既有不重複宣告
- ✓ **time_sync_init 覆蓋 RTC seed**：原 main.cpp setup() STEP 07 才呼叫 `time_sync_init`，若放在 RTC seed 之後會把 seed 結果清空。改為 STEP 06.5 開頭一次 init 完，STEP 07 不再 init

### 已知未測

- ⏳ **永續性（CR2032 備援）**：對時 → 斷電 30 秒 → 重開 → boot log 應顯示 `seeded software clock from RTC: <epoch>` 而非 `等 BLE time_sync`。CR2032 紐扣電池備援能力需實測
- 🐛 **4 個實機 UI bug 待修**（與 DS3231 整合無關，但同時段發現）：見 `tasks/todo.md` 「2026-05-24 實機 bug 待修」段落

### 後續工作

- ⬜ **永續性實測**（5 分鐘可完成）
- ⬜ **4 個 UI bug 修正**（缺字 / 疊字 / 游標位移 / 結束前檢查游標）
- ⬜ **3 項 Phase F 強化測試補完**（中斷重連 / 50 events 滿載 / Training case）— 量產整合測試前補
- ⬜ **React Native App**（取代純網頁的長期方案，與網頁共存）

> 💬 進度 6（BLE 鏈路）+ 進度 7（DS3231 時鐘）合起來 = Phase F 完整收尾。下一個重點是 4 個 UI bug 修正與量產前的硬體驗證。

---

## <a id="progress-2026-05-24"></a>進度 6 — 2026-05-24 — Phase F BLE 鏈路打通端到端（韌體 → 藍牙 → 雲端落地）

### 一句話

**整條同步鏈路打通了**：真實 ESP32 裝置記一筆 OHCA 案件 → 透過藍牙傳到網頁 → 寫進雲端資料庫 → 在網頁可查完整詳細頁。**pm-dev-spec §四 Phase F 必要驗收項目全綠**，階段 1 BLE 鏈路完成。

### 驗收結果（pm-dev-spec §四 Phase F）

| 驗收項目 | 結果 |
|---------|------|
| 同案件同步 2 次，雲端不重複 | ✅ |
| 配對碼 4 位數 + 120 秒過期自動重生 | ✅ |
| 3 次配對失敗鎖定機制 | ✅ |
| 主鍵確認步驟（裝置端按鍵才開傳，雙保險） | ✅ |
| 列表頁所有欄位 ASC/DESC 排序 | ✅ |
| Cloudflare Pages 部署（測試環境） | ✅ |
| 中斷重連 / 50 events 滿載 / Training case 三項強化測試 | ⏸ 留之後（非 spec 必要） |

### 過程中遇到並解決的問題

從第一次燒韌體到端到端通過，連續打通 **6 層 bug**：

- ✓ **裝置中文缺字**：TFT 顯示「同步至 App」「請輸入配對碼」「剩餘 N 秒」等字串缺字。Root cause：字型生成工具只掃 main.cpp，但 UI 字串散落在多個 ui_*.cpp。修法：工具升級接多檔，重生字型補 13 個缺字
- ✓ **macOS Chrome 掃不到裝置**：Chrome 開發者工具看不到 ESP32 廣播。Root cause：藍牙廣播封包中裝置名稱被擠到輔助封包，macOS 略過不解析。修法：顯式安排廣播封包讓裝置名與服務識別碼都進主封包
- ✓ **連線後卡住不動**：點連線後永遠停在「連線中...」。Root cause：Chrome 預設藍牙傳輸大小協商不成功而 hang。修法：韌體主動指定上限值 517 bytes
- ✓ **配對碼通過但網頁沒反應**：裝置螢幕跳「請按主鍵」但網頁 UI 卡在「等待裝置驗證」。Root cause：**對時回應訊息缺結尾換行符**，導致與配對成功訊息黏在同一行被 JSON 解析器拒絕，兩則訊息一起被丟棄。修法：訊息結尾補換行符
- ✓ **長訊息被靜默截斷**：藍牙短訊息超過 20 bytes 會被預設值截斷而韌體不報錯。修法：發送函式內自動依協商值分段
- ✓ **燒檔案系統失敗**：開發工具找不到檔案系統分區。Root cause：分區設定用數字型別，新版工具只認文字型別。修法：分區設定用文字 "spiffs"（雖然實際是 LittleFS）

每個 bug 修完都通過完整的程式碼審查流程（lint → 簡化 → 兩層 PR review 共 4 個 AI agent 並行檢查），最終 commit 含 **1 個 Critical + 6 個 Important 等級 review 發現都修完**，避免問題遺留。

### 已部署測試環境

🌐 **https://ems-dosesync-sync.pages.dev**

PM 可直接：
- 開首頁 → 「連接裝置」（需要 Chrome / Edge + 真實 ESP32 開機）
- 開列表頁看已同步案件
- 點任一案件看 4 頁籤詳細資料（交班摘要 / 完整總覽 / Timeline / 備註）

### 已知小問題（不擋驗收）

- **同步後案件無真實開始時間**：裝置目前無硬體時鐘（RTC），開機後只有相對毫秒數，案件時間欄位填 0，由 App 端用 elapsed_ms 重建。已硬體採購 DS3231 RTC 模組，Dev-Phase 3 上機後可填真實時間
- **主鍵未按 30 秒超時的網頁訊息不精確**：裝置先超時而非網頁端，網頁顯示「裝置斷線」而非「等待逾時」。修法已在 commit 內，下次測試生效
- **3 項強化測試留之後**：中斷重連 / 50 events 滿載 / Training case mode 欄位 — 不在 pm-dev-spec 必要驗收項目內，不擋 Dev-Phase 2 結案

### 後續工作

- ⬜ **DS3231 RTC 整合**：硬體已就位，整合後案件 / 事件可填真實時間戳（Dev-Phase 3 重點）
- ⬜ **3 項強化測試補完**（中斷重連 / 滿載 / Training）— 量產整合測試前補
- ⬜ **React Native App**（取代純網頁的長期方案，與網頁共存）

> 💬 階段 1 BLE 鏈路完成 = Phase F 主要里程碑達成。下一個重點是 DS3231 RTC 整合補上真實時間，以及量產前的硬體驗證。

---

## <a id="progress-2026-05-12-2"></a>進度 5 — 2026-05-12 — Phase F 網頁端完整 UI 落地（雲端 + SoT §17 全套）

### 一句話

做完 **Phase F 的網頁端** 並 **正式部署上雲**，從零做到「**丟一個 URL 給人就能看完整案件**」。**雲端資料庫**（Cloudflare D1）通了，SoT §17 規範的全部介面（4 個頁籤、交班摘要、完整總覽、Timeline、備註、複製、刪除、OHCA/Training 分列表）都做完。下一步是讓 ESP32 韌體真的能透過藍牙連到網頁。

### 線上網址（PM 可立即開瀏覽器查看）

🌐 **https://ems-dosesync-sync.pages.dev**

- 首頁是同步頁（按大按鈕會跑一筆假案件進雲端 DB）
- 案件列表頁：[https://ems-dosesync-sync.pages.dev/cases.html](https://ems-dosesync-sync.pages.dev/cases.html)
- 進入任一案件後可看 4 個頁籤（交班摘要 / 完整總覽 / Timeline / 備註）
- 直接用手機開也可（手機版面已適配，最大寬度 480px）

### 為什麼先做網頁不是 App

| 維度 | 走 App | 走網頁 |
|------|--------|--------|
| 環境啟動成本 | RN 工具鏈 / 模擬器 / 真機簽章 | Chrome 開即可 |
| 藍牙除錯 | 真機 console 不便 | 開發者工具直接看 |
| 迭代速度 | rebuild 慢 | F5 即可 |
| 雲端後端 | 仍需做 | 一次到位 |

**決策**：網頁是藍牙傳輸協定、配對碼流程、資料結構的**驗證載體**，同時擴展成 **V1 完成後測試 + Demo 載體**。給人看 Demo 不用裝 App，URL 一丟即可。正式 App 階段仍會做 React Native App，與網頁共存而非互斥。

### 兩階段一次做完

**階段 1 — 藍牙鏈路骨架（已通）**

- 雲端資料庫（Cloudflare D1）：存案件主表 + 事件序列表，欄位對齊韌體與正式 App
- 雲端 API（Cloudflare Pages Functions）：4 個端點（案件列表 / 單筆 / 刪除 + 備註讀寫），含排序白名單防注入
- 連線頁：手機版面、連線按鈕佔螢幕 1/5、4 格配對碼自動跳格、同步進度即時 log
- 列表頁：時間倒序、欄位可排序、雙欄位時間（可讀格式 + 原始毫秒）
- 假資料同步流程：點按鈕跑模擬流程驗證整條「按按鈕 → 雲端 → 列表頁」鏈路通

**階段 2 — 對齊 SoT §17 完整介面（已通）**

| SoT 規範 | 實作內容 |
|---------|---------|
| §17.2 4 頁籤 | 案件詳細頁：交班摘要 / 完整總覽 / Timeline / 備註 |
| §17.3 交班摘要 | 對齊規範範例格式（總時間 / EPI / 電擊 / Amio + 第一次本機 + 最後本機 + 接手前補登 + 純補登） |
| §17.4 完整總覽 | 14 個欄位 grid 展開 |
| §17.5 備註 | 5 欄表單，1 秒自動存，明顯警告「不會寫回裝置」 |
| §17.6 複製 | 三個按鈕（一鍵摘要 / 完整 Timeline / 列表頁每筆複製） |
| §17.7 刪除 | 二次確認，只刪雲端不影響裝置 |
| §17.8 OHCA / Training 分列表 | 兩個 tab，後端 mode 篩選 |

### 過程中遇到並解決的問題

- ✓ **雲端開發工具版本踩雷** — 升級 wrangler 4.90.1 後仍有資料庫綁定不一致問題：本機 migration 命令與本機開發伺服器命令竟然指向不同的本機 SQLite 檔案，造成 migration 跑完了但網頁查不到表。解法：把設定檔的資料庫 ID 從文字 placeholder 改成標準 UUID 格式，並移除 dev 腳本的多餘旗標讓設定檔自己處理綁定。已存進團隊記憶庫供後續專案避雷
- ✓ **規格漏洞補上** — 重看 SoT §16.5 才發現原本網頁計畫漏寫「裝置端按主鍵確認」這一步。**雙保險**：配對碼防誤連、主鍵確認防操作者還沒準備好就開傳。已補回網頁計畫與韌體 todo
- ✓ **配對 UX 釐清** — 跟使用者確認後敲定流程：「配對碼由裝置螢幕顯示、使用者在網頁鍵盤輸入回送，不需要在裝置上輸入」。避免韌體誤實作數字按鈕輸入介面

### 雲端落地方式

- **正式網址**：[https://ems-dosesync-sync.pages.dev](https://ems-dosesync-sync.pages.dev)（Cloudflare Pages，全球 CDN + HTTPS 自動配置）
- **開發階段**：本機跑 Cloudflare 模擬器（`npm run dev`），帶假 UUID 不會佔用真實雲端資源
- **雲端部署**：寫了 `deploy.sh` 一鍵腳本，會先檢查設定檔不是 placeholder 才繼續，避免誤推
- **免費額度**：Cloudflare D1 提供 5GB 儲存 / 每天 500 萬筆讀取，對救護紀錄量綽綽有餘

### 後續工作

- ⬜ **F-1** 韌體配對碼模組 TDD（0.5 天）— 純邏輯不依賴硬體，可邊做邊配合網頁驗證
- ⬜ **F-2** 韌體藍牙傳輸協定（NimBLE NUS）（1.5 天）
- ⬜ **F-3** 韌體同步流程狀態機（含主鍵確認）（1.5 天）
- ⬜ **F-4a** 網頁真連手機 nRF Connect 假裝置（0.5 天）
- ⬜ **F-4b** 網頁連真韌體（0.5 天）
- ⬜ **F-7 + F-8** 端到端整合 + SoT §四 Phase F 驗收（1.5 天）

> 💬 下一個開工項目是韌體配對碼模組（純邏輯），可邊做邊配合網頁部分驗證。目前剩約 6 工作天可全部完成 Phase F。

---

## <a id="progress-2026-05-12"></a>進度 4 — 2026-05-12 — V1 各階段進度盤點＋測試基礎建設到位

### 一句話

把 SoT V1 規格的 **8 個階段（A~H）** 跑完一次盤點，**4 個完成 4 個待做**；同時把擋下「燒進韌體才發現問題」的**自動化測試框架**建好 6 層，下一個重點是 **Phase F（藍牙傳手機 App）**。

### V1 規格 8 個階段完成度

| 階段 | 功能 | 狀態 | 完成日 |
|------|------|------|--------|
| **A** | OHCA 核心（EPI 4 分倒數 / 兩段確認 / 預警警報超時） | ✅ 完成 | 2026-05-04 |
| **B** | 補登（EPI / Amio）/ 案件總覽 / 事件時間軸 | ✅ 完成 | 2026-05-08 |
| **C** | 6 秒通氣節奏（獨立模式 + OHCA 內輔助） | ✅ 完成 | 2026-05-05 |
| **D** | Training 訓練模式 | ⬜ 未開工 | — |
| **E** | 資料持久化 + 歷史紀錄（重開機資料不遺失） | ⚠️ 軟體完成、缺硬體零件 | 2026-05-12 |
| **F** | App 配對碼同步（藍牙傳手機 App） | ⬜ 未開工 | **下一個重點** |
| **G** | 系統設定 + Type-C 管理工具（列案件 / 匯出 / 清除） | ⬜ 未開工 | — |
| **H** | 電源管理（充電拓樸 / 邊充邊用 / 低電量警告） | ⏸ 阻塞中，等零件 | 等採購 |

> 已完成：**3 / 8 全綠**（A / B / C）｜**1 / 8 半綠**（E 軟體面 OK 但缺硬體零件）｜剩餘：D / F / G / H

### ⚠️ 卡採購的兩個階段

| 階段 | 韌體狀態 | 卡在哪 | 需要的零件 |
|------|---------|--------|----------|
| **E（持久化）** | 韌體完成、能寫入也能讀回 | **沒有絕對時間戳記** | DS3231 即時時鐘模組（I2C 接線，給每筆事件記真實年月日時分秒） |
| **H（電源管理）** | 完全未開工 | **沒有電池就沒有充放電可管** | TP4056 充電 IC、升壓 IC、3.7V 鋰電池、單節保護板 |

**Phase E 現況補充**：目前每筆事件的時間欄位記的是「從這次開機起算的毫秒數」，重開機後相對時間還在但無法對應到真實日期時間。**短期變通**是 Phase F 完成後讓手機 App 連線時下發 epoch 時間做軟體對時；**長期**仍建議買 DS3231 模組焊上去，因為救護現場可能 App 未連線就要記錄事件。

**Phase H 阻塞**：採購到貨後才有東西可以做。採購清單已整理在 `docs/hardware-procurement-v2.html`（PM 可直接看那份決定下單）。在電源模組到位之前，主機只能 USB 直供，量產殼也沒法定型。

### 測試基礎建設 6 層

Phase E 持久化開發時撞到「燒進韌體才發現分區配置不同步、開機就無限重啟」的問題，當下用了不少時間查根因。為避免重複踩同樣的雷，這幾天把測試框架補齊到 6 層，未來新功能燒板前能先被自動化擋下。

| 層 | 抓什麼問題 | 狀態 | 完成日 |
|---|------------|------|------|
| 1️⃣ 建置時驗證 | 分區配置超出 Flash 容量 / 韌體 bin 不完整 | ✅ | 2026-05-12 |
| 2️⃣ 整合測試 | 顯示畫面新增欄位漏同步（Phase E 那種 regression） | ✅ | 2026-05-12 |
| 3️⃣ 實機測試 | 真寫進 Flash → 重新掛載 → 資料還在 | ✅ | 2026-05-12 |
| 4️⃣ 靜態分析 | 死碼 / 隱式型別轉換 / 變數遮蔽 | ✅ | 2026-05-12 |
| 5️⃣ 開機健檢 | 燒完開不了機 / boot loop / panic | ✅ | 2026-05-12 |
| 6️⃣ 預算監控 | Flash / RAM 用量爆出預算（量產時硬殼會塞不下） | ✅ | 2026-05-12 |

目前主韌體實際用量：**Flash 27.1%（533 KB / 1.9 MB）｜RAM 16.4%**，距離量產上限充裕。

### 後續工作

- ⬜ **Phase F**（App 配對碼同步）— 下一個開工重點，這是讓救護資料能跨裝置查閱的關鍵
- ⬜ Phase D（Training 訓練模式）— 不阻擋 F，但完成後可給新人單獨練習用
- ⬜ Phase G（系統設定 + Type-C 工具）— F 之後跟進
- ⬜ Phase H（電源管理）— 量產前最後一階段，等實機功耗測完

> 💬 接下來開 Phase F 需要 PM 提供：(1) 配對碼互動 UX 確認；(2) 手機 App 那邊由誰負責、用什麼平台；(3) 第一版資料同步要不要先支援單機歷史紀錄查詢還是直接全雲端。

---

## <a id="progress-2026-05-11"></a>進度 3 — 2026-05-11 — 用自動化測試掃出 10 個畫面文字溢出並修正

### 一句話

請瀏覽器自動測試工具**逐一打開線上 demo 的 34 個畫面**做檢查，找出 **10 個畫面的文字會被螢幕邊界切掉**（含「電擊紀錄整段看不到」、「不知道按哪鍵確認」這種影響操作的嚴重問題），全部一次修完並重新自動驗證 34/34 通過。

### 修了哪些畫面（依嚴重程度排序）

| 畫面 | 修前狀況 | 修後 |
|------|----------|------|
| **OHCA 案件總覽** | 「電擊」剛起頭就切、「Amiodarone」整段完全消失 | 三段（EPI／電擊／Amio）全部塞進螢幕 |
| **恢復預設設定？對話框** | 「主鍵確認 ｜ 返回取消」操作提示消失 → **使用者不知道按什麼確認** | 操作提示釘到底部，永遠可見 |
| **補登次數選單（接手前 EPI）** | 選 ×4 / ×5 時看不到選項，但游標可選到 → **會選錯而不自知** | 5 項全部塞進螢幕，選中項反白 |
| **6 秒通氣節奏起始頁** | 底部「主鍵短按暫停／長按 0.75s 結束」提示完全消失 | 提示釘到底部 |
| **系統設定主選單** | 5 項只看到前 3 項完整 + 第 4 項剩半字，「裝置資訊」「恢復預設值」完全消失 | 5 項都看得到，字級略縮 |
| **重置訓練？對話框** | 底部操作提示被推出畫面外 | 提示釘底 |
| **訓練歷史紀錄操作選單** | 4 項中第 4 項「返回」剩半個字 | 4 項全部完整 |
| **刪除訓練紀錄？對話框** | 同上操作提示問題 | 提示釘底 |
| **裝置資訊頁** | 底部「Demo 限制」說明 + 操作提示消失 | 全部可見 |
| **事件時間軸** | 事件超過 7 筆時，最後幾筆會被切（含「案件結束」事件） | 加上下翻頁，顯示頁碼如 (1-7/9)，▲▼ 捲動 |
| **主選單** | 第 5 項「系統設定」底部約 2 像素被切（不影響閱讀但邊緣切到） | 完全乾淨 |

### 自動化測試怎麼做的

1. 啟動瀏覽器自動化工具開啟線上 demo
2. 透過 demo 內建的畫面切換函式逐一切到 34 個畫面（含 dialog、子選單、補登流程等）
3. 對每個畫面所有可見文字元素做位置計算，比對是否超出 320×240 模擬螢幕邊界
4. 自動截圖溢出的畫面供比對
5. 修完後重跑同一套測試，確認 34/34 都通過

### 過程中遇到並解決的問題

- 第一輪修完還剩 OHCA 案件總覽 5 像素溢出 — 字級從 14 縮到 13、行高拉緊，剛好塞進 224 像素可用區域。
- 事件時間軸事件多時一頁塞不下 — 加上下翻頁機制，每頁顯示 7 列，標題旁顯示頁碼如 `(1-7/9)`，▲▼ 捲動。
- 對話框（重置訓練 / 刪除紀錄 / 恢復預設）三個畫面結構雷同 — 抽出共用樣式「對話框三段釘底版面」，標題、內文、操作提示各自定位，未來新增對話框直接套用。
- 主選單 5 項邊緣切 2px — 把每列高度從 30px 微縮到 28px，現在留有 17px 安全帶。

### 線上 demo（PM 可開瀏覽器互動）

- 部署版本：<https://1d0d3dcd.ems-dosesync-demo.pages.dev>

修完後請 PM 重新打開 demo 點過每個畫面（特別是案件總覽 / 補登次數選 ×5 / 任一恢復預設 / 通氣節奏起始頁），驗收實際效果。

### 後續工作

- ⬜ Phase B（補登／Amio／結束前檢查／案件總覽）程式碼已 100% 落地，待 PM 實機完整流程驗收。
- ⬜ Phase C（6 秒通氣節奏）程式碼已落地，5 項驗收項目待 PM 實機拍板。
- ⬜ Phase D（訓練模式）尚未開工。
- ⬜ Phase E（資料持久化／歷史紀錄）尚未開工，這是下一階段藍牙傳輸到手機 App 的前置必修。

> 💬 demo 視覺問題清乾淨後，下一個重點仍是「重開機後資料不遺失」（Phase E），然後才是手機 App 同步（Phase F）。

---

## <a id="progress-2026-05-09-2"></a>進度 2 — 2026-05-09 — 補登次數選單列表化＋介面文字溢出修整

### 一句話

把「補登 EPI 幾次」畫面改成**像清單一樣的選單**（之前是大數字 1/2/3/4/5 上下調），同時修了「Amiodarone 已紀錄」提示和子選單底部三段操作提示**文字超出螢幕邊界**的問題。

### 你會看到什麼差異

1. **補登次數變成清單樣式**：長按 EPI → 選「補登 EPI」→ 接手前 / 純補登 → 進入次數畫面，現在顯示 5 列（接手前 ×1～×5）或 3 列（純補登 ×1～×3），上下鍵滑動、主鍵確認，跟線上 demo 完全一致。
2. **Amiodarone 提示完整顯示**：之前確認 Amio 後跳出的「Amiodarone 已紀錄」字太大會切到螢幕外，現在自動縮到剛好。
3. **子選單底部提示重排**：以前「上下選擇　主鍵確認　返回取消」全擠在中間、左右會被切；現在改成**左 / 中 / 右三段分排**，貼邊 10 像素，視覺更清楚也不再溢出。
4. **內部進度標籤對齊現況**：程式碼開頭的階段註解全面重寫，把已落地的補登 / 總覽 / Amio 等子流程標為 ✅；把因升級螢幕而被佔用的腳位（震動馬達讓位給彩色螢幕）也註明清楚。

### 過程中遇到並解決的問題

- 第一次修「Amiodarone 已紀錄」只降一級字仍然爆字 — 增開**更小一級的字級檔位**專供英文+中文混合的長標題使用。
- 補登次數第一版只把大數字下移避免擋到「次數(1~5)」標籤 — PM 回饋「跟 demo 不一樣」，第二版整個改成清單樣式對齊 demo。
- 子選單底部三段提示縮字仍擠 — 改成**三段分別貼左 / 置中 / 貼右**，字級縮 0.85 倍，邊距留 10 像素。
- 程式碼註解一度誤寫「× 改 ASCII x 避免字型缺失」— 實機驗證字型有 ×，且 Amio 確認 / 補登成功畫面也已用 ×，改回 × 並修正註解一致性。
- 線上 demo 主選單原本只能看到 4 個項目（第 5 個「系統設定」被螢幕底邊切掉）— 已修，把每列高度從 48px 縮成 38px，5 項合計 217px 在 240px 螢幕內完整顯示。

### 線上 demo（PM 可開瀏覽器互動）

- 部署版本：<https://1d0d3dcd.ems-dosesync-demo.pages.dev>

實機已燒錄韌體，補登流程跟 demo 並排對照可看到完全一致的清單樣式。

### 後續工作

- ⬜ Phase B（補登／Amio／結束前檢查／案件總覽）程式碼已 100% 落地，待實機完整流程驗收。
- ⬜ Phase C（6 秒通氣節奏）程式碼已落地，5 項驗收項目待 PM 實機拍板。
- ⬜ Phase D（訓練模式）尚未開工。
- ⬜ Phase E（資料持久化／歷史紀錄）尚未開工，這是下一階段藍牙傳輸到手機 App 的前置必修。

> 💬 Phase A/B 的核心急救流程已可用，下一個重點是把「重開機後資料不遺失」做好（Phase E），然後才是手機 App 同步（Phase F）。

---

## <a id="progress-2026-05-09"></a>進度 1 — 2026-05-09 — 螢幕升級到 2.8 吋並修整體美觀

### 一句話

把舊的 0.96 吋小螢幕換成 **2.8 吋大螢幕**，整個操作畫面重畫，文字放大、中文字型升級，畫面切換感變順、不再有以前那種掃描閃爍。

### 你會看到什麼差異

1. **螢幕變大很多**：從原本郵票大小變成手掌心大小，遠遠就能看清倒數秒數。
2. **字級放大約 1.5 倍**：之前你反映過字看不清楚，這次主畫面字放大、間距重抓。
3. **中文字看起來更舒服**：「電擊」「給藥」這種筆畫密的字以前看起來歪歪的，現在換了字型工整很多。
4. **畫面切換不再閃**：以前切畫面會看到一條條掃描線像跑馬燈，現在切畫面接近瞬完成。
5. **操作流程跟線上 demo 一致**：13 個畫面（主選單、OHCA 倒數、待機、結束確認、6 秒通氣節奏…）都對齊 demo 的視覺，可以拿手機開 demo 並排對照。

### 硬體規格

| 項目 | 規格 |
|------|------|
| **顯示器** | 2.8 吋 TFT 彩色螢幕（型號 ST7789） |
| **解析度** | 320 × 240 像素 |
| **接線方式** | SPI 4-wire，運行頻率 80 MHz |
| **主控板** | ESP32-S3（GOOUUU N16R8，含 8MB PSRAM） |
| **功耗** | 典型 80 mA / 峰值 120 mA（含 TFT 背光） |
| **韌體佔用** | Flash 14.1%（約 470 KB / 3.34 MB）｜RAM 6.8%（22 KB / 320 KB） |
| **中文字型** | 自訂 24px 點陣字，222 個常用字含全形標點，91 KB |

### 過程中遇到並解決的問題

- 開機時主板上的 RGB LED 會亮一下白光（不影響功能但 PM 看了奇怪）— 已修，開機流程主動把 LED 關掉。
- 換螢幕後發現顯示驅動跟主板某顆 LED 共用同一條控制線 — 已重新分配腳位避開衝突。
- 第一版字級雖然放大但部分中文字（如「電擊」）筆畫密、看起來有歪斜感 — 換成自訂字型後解決。
- 螢幕切換每次都會看到掃描線往下跑 — 改用直接記憶體推送 (DMA) 整片送，現在切換接近瞬完成。
- 倒數時某些情境下按鍵會被連續觸發兩次 — 已修，按鍵防彈跳改用統一門檻。

### 線上 demo（PM 可開瀏覽器互動）

- 部署版本：<https://1d0d3dcd.ems-dosesync-demo.pages.dev>
- 分支版本：<https://feat-tft-integration.ems-dosesync-demo.pages.dev>

實機已燒錄韌體，可現場拍照對照 demo。

### 後續工作

- ⬜ Backfill / Drug 子選單實機操作確認（按鍵流程）
- ⬜ 6 秒通氣 4 種訊息對齊 demo（已寫好程式，待實機看視覺）
- ⬜ Dialog 對話框內部座標可優化（不影響功能）
- ⬜ 第二套中文字型工具測試（目前只有一套，當未來想換字型時做）

> 💬 以上後續工作都不阻擋下一階段「藍牙傳輸到手機 App」開工。

---

*本報告會在每次重要進度後累加。最新進度排在最上方。*
